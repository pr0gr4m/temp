# Makefile - General Makefile for build
#

# include gerneral Makefile configuration file
include Makefile.inc

# execute file list
EXE = msg_sender msg_receiver

all : ${EXE}

clean : 
	${RM} *.o ${EXE}

showall :
	@ echo ${EXE}

${EXE} :
